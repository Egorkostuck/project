import { MantineStyleProp, rem } from '@mantine/core';

export const iconSize = 24;
export const iconSizeRem = rem(iconSize);
export const styles: MantineStyleProp = {
  position: 'absolute',
  top: '100px',
  right: '50px',
  width: '300px',
  height: 'auto',
};
