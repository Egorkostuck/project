export enum InputValidation {
  Length = 'Name must have at least 2 letters',
  Invalid = 'That name already exists',
}

export enum Fields {
  Name = 'name',
}

export enum TypeButton {
  Submit = 'submit',
}
